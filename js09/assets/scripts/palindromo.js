const array = [];
const palabra = "Ana";
const 

const convierteArray = (palabra) => {
    return array = palabra.split("");
}


const palindromo = (array) => {
    let checarLetra = false;
    for (letra in array)
        if (array[i] == array[j]) {
            let checarLetra = true
        else {
            console.log("No es un palíndromo")
            break;
        }
        }
            
}
